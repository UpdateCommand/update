# Yum package manager for RedHat Linux, CentOS Linux, etc.
#
if command -v yum >/dev/null 2>&1; then
  sudo yum --assumeyes clean
  sudo yum --assumeyes --obsolete update
  sudo yum --assumeyes clean
fi
