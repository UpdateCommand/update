#!/bin/sh
#
# Run update commands for everything we use on all platforms.
#
# Heuristics for ordering:
#
#   1. Update OS e.g. Ubuntu `do-release-upgrade`, macOS `softwareupdate`.
#
#   2. Update system packages e.g. Debian `apt`, RedHat `yum`, macOS `brew`.
#
#   3. Update shells e.g. Z Shell.
#
#   4. Update shells and langauges e.g. Z Shell, Rust.
#
#   5. Update tool packages: e.g. Node `npm`, Python`pip`, Ruby `gem`.
#
#   6. Update repositories: `/opt`, `~/git`, etc.
#
# Guidance:
#
#   * Each script should be testing that it has what it needs to run.
#     This includes testing for any platform-specific needs.
#
#   * Each script should try to clean before running and after running.
#     This helps create disk space, and helps maintain dependencies.
#
# We welcome patches, pull requests, and ideas.
#
# Version: 4.3.3
# Created: 2015-06-01
# Updated: 2018-03-01
# License: GPL
# Contact: Joel Parker Henderson (joel@joelparkerhenderson.com)
##
set -euf

## Update operating system
update-ubuntu-release || :
update-macos || :

## Update system packages
update-apt || :
update-brew || :
update-brew-cask || :
update-brewfile || :
update-dnf || :
update-mas || :
update-pacman || :
update-yum || :

# Update shells
update-oh-my-zsh || :

# Update languages
update-rustup || :

# Update tool packages
update-apm || :
update-cargo || :
update-gem || :
update-gemfile || :
update-pip || :
update-pod || :
update-podfile || :
update-npm || :
update-cabal || :
update-motion || :

# Update repositories
update-repos || :
