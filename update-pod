# Pod: Cocoapod package manager for macOS

if command -v pod >/dev/null 2>&1; then
  dir="${XDG_CONFIG_HOME:-$HOME/.config/pod}"
  if [ -r "$dir" ]; then
    pod update --project-directory="$dir"
  fi
fi
